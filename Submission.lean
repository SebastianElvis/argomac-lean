/-
This file is the entry point of the submission.
`challenge.yaml` names `Submission.solution` as the entry.
The construction is computable and the correctness proof is closed.
The direct adaptive-privacy proof is open, so no `solution` inhabits
`Kriterion.Solution` yet.
-/

import Construction
import Proof
